# PROJECT_CONTEXT.md — ルナ🌙ガチャ

このファイルは、Codexが必要時に参照するための軽量プロジェクト設定集です。
常時ルールは `AGENTS.md`、今回の作業内容は個別依頼を優先してください。

## 1. プロジェクト方針

- 横画面（landscape）を正式版・基準実装として開発する。
- 縦画面版は完成済み参照版として扱い、原則変更しない。
- Webで端末回転を強制する前提にはしない。
- 既存の正常動作・完成素材を壊さず、小さく安全に進める。
- 実ブラウザ / iPhone確認はユーザー側。Codexは静的・論理・ファイル検査を行う。

## 2. 現行ガチャ仕様

- 初期所持石: 3000
- 1回召喚: 300
- 10連召喚: 3000
- 無料チャージ: +3000

レアリティ確率:
- N: 50%
- R: 30%
- SR: 14%
- SSR: 5%
- UR: 1%

## 3. 10連特殊演出 — 正式仕様

### UR × 2以上
- チビルナ劇場プレミア 100%
- 動画: `assets/video/chibiluna_premium_theater.mp4`

### UR × 1
- 幻想譚 100%
- 動画: `assets/video/gensotan_ur1.mp4`

### URなし・SSRあり
- 50%で特別演出判定
- 特別演出に入った場合:
  - 50% 荘厳系
  - 50% 既存の通常 / サプライズ側
- 荘厳系動画: `assets/video/solemn_ssr.mp4`

### URなし・SSRなし
- 既存の通常演出

古い「UR×1 = 荘厳 / 劇場 50:50」は誤仕様。使用しない。

## 4. 完成済み動画素材

以下は完成済みWeb用素材。
明示指示なしに再エンコード・編集・差し替えしない。

- `assets/video/gensotan_ur1.mp4`
- `assets/video/chibiluna_premium_theater.mp4`
- `assets/video/solemn_ssr.mp4`

## 5. 既存結果フロー

どの演出ルートから入っても、最終的に既存のカード開示・結果画面へ戻す。

壊してはいけないもの:
- 10連結果
- collection登録
- stones
- pull count
- 既存の正常な通常演出

collectionは現行で localStorage `luna_gacha_collection_v1` を使用。

## 6. 現在の実装状況

基準は v5.63 系。

確認済みの主要事項:
- `index.html` 単一HTML/CSS/JS構成
- 確率は N50 / R30 / SR14 / SSR5 / UR1
- 10連は「10枚生成 → collection登録 → 演出 → 結果」
- `#stones` と `#heroStones` は同じ stones 値を表示

前回Codex作業は使用上限で中断。
中断直前:
- `origin/main` に動画3本があることを確認
- 3動画をローカル `assets/video/` に安全に取り込んだ
- 10連特殊演出の最新分岐実装へ着手する直前〜途中で停止した可能性あり

再開時は必ず現在のworktreeと未コミット変更を確認し、
既存変更を壊さず中断地点から続ける。

## 7. 既知課題 — 今回無関係なら触らない

- top画像内に固定表示された「月のかけら 2900」
- 動的DOMの石表示との二重UI
- DEV表示 / 演出強制UI
- 旧result系DOMと `#resultOverlay` の共存
- 未使用関数 / 旧シーケンス
- 一部素材パス / フォルダ名の問題
- 古い説明文やコメントとの不整合
- SKIP / audio / race の実ブラウザ確認

「見つけたから直す」はしない。必要なら報告のみ。

## 8. Codexモデル運用

- Terra: 標準。通常の機能追加、JS修正、演出分岐、HTML/CSS、通常バグ修正。
- Luna: 軽作業。文字、数値、ファイル名、参照パス、短い明確な修正。
- Sol / Astra: 難所のみ。原因不明の難しい問題、複数システム横断、大規模設計変更、Terraで解決できない場合。

## 9. 参照優先順位

1. 今回のユーザー / チャピの明示指示
2. `AGENTS.md`
3. `PROJECT_CONTEXT.md`
4. 現行コード / コメント

古いコメントや旧仕様より、最新の明示指示を優先する。
