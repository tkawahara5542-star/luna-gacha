# AGENTS.md — ルナ🌙ガチャ Codex運用ルール

このファイルは、Codexがこのリポジトリで作業するときの常設ルールです。
個別タスクの最新指示がある場合は、その指示を優先してください。

## 1. 最小変更

- 1タスク1目的を基本とする。
- 指示された目的に必要な範囲だけ読む・変更する。
- 無関係なリファクタ、命名変更、CSS整理、未使用コード削除、構造変更を行わない。
- 「ついでの改善」は実装せず、必要なら報告だけする。
- 既存の正常動作・完成素材を、明示指示なしに変更しない。

## 2. 中断・再開

前回作業が途中の可能性がある場合は、実装前に必要最小限の範囲で以下を確認する。

- worktree
- 未コミット変更
- 対象ファイルの現在状態
- 前回変更がどこまで反映されているか

既存作業を残したまま続きを行う。

禁止：
- 未コミット変更の破棄
- 勝手な reset / checkout による上書き
- 状態確認前のやり直し
- 安易な `git pull`

リモート確認が必要な場合は、worktreeを壊さない方法を優先する。

## 3. 調査・検査

- 毎回リポジトリ全体を調査・総点検しない。
- 対象箇所と直接の影響範囲を優先する。
- 通常は、変更箇所・呼び出し関係・分岐・ファイル参照・既存フローへの復帰・構文を確認する。
- 全体総点検は明示指示がある場合のみ行う。
- ブラウザ・実機で確認していない場合は「ブラウザ実機確認は未実施」と明記する。
- 実施していない確認を「確認済み」と表現しない。

## 4. Git・成果物

- Codex作業の正式成果物はGitHub更新を基本とする。
- 変更対象外のファイルを触らない。
- 完成済み画像・動画・音声などの素材を、明示指示なしに再生成・再エンコード・差し替えしない。
- ZIPはユーザーから明示的に求められた場合のみ作成する。

## 5. 報告

報告を求められた場合は、Codexチャット本文に報告内容を全文記載する。

報告ファイルも作成する場合：
- チャット本文と報告ファイルを同一内容・同一構成にする。
- 省略・要約・差し替えをしない。

報告には必要に応じて以下を含める。

- 変更ファイル
- 変更内容
- 確認済み事実
- 未確認事項
- 🔴 今すぐ
- 🟡 次
- 🟢 後回し
- 次に着手するべき1件

確認済み事実と、判断・提案は区別する。

## 6. 優先順位

競合する情報がある場合は、原則として次の順に優先する。

1. 今回のユーザー／チャピからの明示指示
2. この `AGENTS.md`
3. リポジトリ内の現行実装・コメント

古いコメントや過去仕様を、最新の明示指示より優先しない。

---

目的は、Codexの利用量を抑えながら、既存の良い部分を壊さず、小さく安全に開発を進めることです。
