# v5.63 演出分岐アップデート報告書

作業日: 2026-09-12
対象: `index.html`、`assets/video/`
実機ブラウザテスト: 未実施

## 1. 実装した内容

### 事実として確認できたこと

- `origin/main` から、作業ツリーの既存未コミット変更に触れず、次の完成動画3本だけを `assets/video/` へ抽出しました。
  - `gensotan_ur1.mp4`
  - `solemn_ssr.mp4`
  - `chibiluna_premium_theater.mp4`
- `revealTen()` の10連分岐を、UR枚数を最優先に判定する構成へ変更しました。
  - UR×1は `playGensotan()` を100%呼びます。
  - UR×2以上は `playPremiumComical()` を100%呼びます。
  - URが0枚でSSRを含む場合だけ、50%で特別演出判定を行います。特別演出時はさらに50%で `playSolemnVideo()`、残り50%で既存の通常カード開示へ進みます。
- 動画演出用の `playTenVideo()` を追加しました。動画の終了後は既存の `revealTenFanSequence()`、`finishNormal()`、`showResultOverlay()` の経路へ進みます。SKIP時は既存の `finishSkip()` へ進みます。
- UR×1の旧「50%荘厳 / 50%チビルナ劇場」分岐を除去しました。
- `playPremiumComical()` の既存劇場演出への委譲を、プレミア動画 `chibiluna_premium_theater.mp4` の再生へ置き換えました。
- 画面注記を最新正式仕様と矛盾しない内容へ更新しました。

### 判断・注意点

- SSR（URなし）の通常側サプライズには、既存の通常10連カード開示を接続しています。専用の「サプライズ」関数・映像は現行コード内に存在しないため、新規演出は作成していません。
- 動画の再生失敗時も、10連結果画面へ進まなくなることを避けるため、既存のカード開示・結果表示へフォールバックします。

## 2. 変更したファイル

- `index.html`
  - 最新仕様に合わせた画面注記。
  - 動画再生後に既存開示へ戻る `playTenVideo()`。
  - 幻想譚、SSR荘厳系、チビルナ劇場プレミアの接続関数。
  - 最新仕様に従う `revealTen()` の分岐。
- `assets/video/gensotan_ur1.mp4`
- `assets/video/solemn_ssr.mp4`
- `assets/video/chibiluna_premium_theater.mp4`
- `v5.63_演出分岐アップデート報告書.md`

## 3. 変更していないファイル・素材

### 事実として確認できたこと

- 排出率、カードプール、石消費、10枚生成、図鑑登録、図鑑仕様、結果画面、カード詳細、既存の通常開示、既存の `playComical()` の劇場内容、HTML構造、既存CSS、既存画像、既存SEは変更していません。
- 動画3本は `origin/main` のGitオブジェクトからそのまま抽出しており、編集・再エンコードしていません。
- 関係のない旧コード整理・CSS整理は行っていません。

## 4. 正常確認できた項目

### 事実として確認できたこと

- JavaScript構文チェックは成功しました。
- 3本の動画は指定パスに存在し、`origin/main` 上の各ファイルサイズと一致します。
- コード上の分岐は次のとおりです。

| 条件 | 接続先 |
| --- | --- |
| N/R/SRのみ | 既存の通常10連カード開示 |
| SSRあり・URなし | 50%で特別演出判定。特別時は50%でSSR荘厳動画、50%で既存通常開示 |
| UR×1 | 幻想譚動画 `gensotan_ur1.mp4` 100% |
| UR×2以上 | チビルナ劇場プレミア動画 `chibiluna_premium_theater.mp4` 100% |

- すべての動画分岐は動画終了後、既存のカード開示を経由して既存の10連結果画面へ接続します。
- 既存の10枚生成と図鑑登録は `revealTen()` の前に完了するため、動画分岐の追加による変更はありません。

## 5. 問題・注意点

### 事実として確認できたこと

- 動画再生はブラウザの自動再生ポリシーの影響を受ける可能性があります。
- 現行のSSR通常側サプライズは、既存の通常カード開示へ接続しています。専用のサプライズ映像・関数は追加していません。

### 判断・注意点

- 動画に音声が含まれる場合、ユーザー操作から非同期処理を経た再生がブラウザで許可されるかは実機確認が必要です。
- 動画ロード失敗時は結果到達を優先して通常開示へ進むため、利用者には動画演出が表示されません。エラー表示を加えるかは別途仕様判断が必要です。

## 6. 実機未確認項目

- PCおよびモバイルブラウザでの動画自動再生・音声再生。
- 動画終了時とSKIP時の結果画面遷移。
- UR×1、UR×2以上、SSRのみの実排出時の表示・音声・スクロール状態。
- 低速回線時の動画ロードとフォールバック動作。

## 7. 実施した検査

### 事実として確認できたこと

- `index.html` のJavaScript構文チェックは成功しました。
- 分岐マトリクスを静的に確認しました。
  - UR×2 → プレミア動画
  - UR×1 → 幻想譚動画
  - URなしSSRあり・特別判定・荘厳側 → 荘厳動画
  - URなしSSRあり・特別判定・通常側 → 既存通常開示
  - URなしSSRあり・特別判定なし → 既存通常開示
  - URなしSSRなし → 既存通常開示
- 3動画のGit blobハッシュは `origin/main` 上のファイルと一致しました。
- `git diff --check` は問題を報告しませんでした。

## 8. プロジェクト文書とGitHub反映状況

### 事実として確認できたこと

- 指示された `AGENTS.md` と `PROJECT_CONTEXT.md` は、ローカル作業ツリーにも `origin/main` にも存在しませんでした。そのため当該文書の個別ルールは確認できませんでした。
- 動画素材は `git fetch origin main` 後、`origin/main` から指定の3ファイルだけを抽出しました。既存の未コミット変更をreset、checkout、pullで破棄・上書きしていません。
- GitHub反映は完了しています。実装コミット `f2e5e6b` を `origin/main` へpushしました。既存の `codex_report.md`、総点検報告書、および今回の目的と無関係な変更は含めていません。

### 判断・注意点

- 恒久ルール文書がリポジトリに存在しないため、今後は正本の配置場所を明確にすることが望まれます。

## 9. 🔴今すぐ

- PCとモバイルで、3動画の再生、終了後の10枚開示、SKIP、結果画面到達を実機確認する。

## 10. 🟡次

- SSR通常側サプライズを、既存通常開示のままとするか、専用視覚演出を追加するかを正式に確定する。
- 動画の再生失敗を利用者へ表示する必要があるかを決める。

## 11. 🟢後回し

- 旧演出コード、旧画面注記以外の旧コメント、未使用素材、CSS重複の整理。

## 12. 次に着手するべき1件

**3本の動画演出を含む10連分岐のブラウザ実機試験を行う。**

理由: 静的接続と素材配置は確認済みですが、動画自動再生、音声、終了イベント、SKIPの挙動は実機ブラウザでのみ検証できるためです。
