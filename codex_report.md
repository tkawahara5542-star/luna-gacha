# ルナガチャ修正報告

作業日: 2026-09-11  
対象: `index.html`

## 変更内容

石表示の同期のみを修正しました。

- `heroStonesEl` として `#heroStones` のDOM参照を追加しました。
- `updateStatus()` に `heroStonesEl.textContent=stones` を追加しました。

これにより、初期化時、単発召喚、10連召喚、無料チャージ、および各処理完了時に呼ばれる既存の `updateStatus()` を通じて、ヒーロー上部と通常ステータスの石表示が同じ `stones` 値になります。

## 検証結果

- JavaScriptの構文チェックは成功しました。
- `#stones` と `#heroStones` が同じ `stones` 値を設定する更新処理に含まれることを確認しました。
- 変更差分は `index.html` の石表示DOM参照と `updateStatus()` の2箇所、および本レポートのみです。

## 未確認事項

- ブラウザ上で実際に単発召喚・10連召喚・無料チャージを操作するUI試験は実施していません。
- ご指定のとおり、演出、画像、SE、素材パス、旧コード、10連の流れには変更を加えていません。
